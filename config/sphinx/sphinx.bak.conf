#
# Sphinx configuration file sample
#
# WARNING! While this sample file mentions all available options,
# it contains (very) short helper descriptions only. Please refer to
# doc/sphinx.html for details.
#

#############################################################################
## data source definition
#############################################################################

source obuvua_base
{
    type = mysql
    sql_host = localhost
	sql_user = homestead
    sql_pass = secret
    sql_db = obuvua
}
	
source kapot_base
{
    type = mysql
    sql_host = localhost
	sql_user = homestead
    sql_pass = secret
    sql_db = kapot2
}

source ag_base
{
    type = mysql
    sql_host = localhost
	sql_user = homestead
    sql_pass = secret
    sql_db = agrobiz
}

source navolyni_base
{
    type = mysql
    sql_host = localhost
	sql_user = homestead
    sql_pass = secret
    sql_db = navolyni
}

source lvivmarket_base
{
    type = mysql
    sql_host = localhost
	sql_user = homestead
    sql_pass = secret
    sql_db = lvivmarket
}

source base
{
    type = mysql
    sql_host = localhost
	sql_user = homestead
    sql_pass = secret
    sql_db = bigopt
}



###------------------------------------------------------------------------
###---------- OBUVUA

source obuvua_goods : obuvua_base
{

	sql_query_pre 		= SET NAMES utf8
	sql_query_range 	= SELECT MIN(id), MAX(id) FROM goods
	sql_range_step 		= 500
	
	sql_query = \
		select \
			id, \
			name, \
			keywords, \
			about, \
			'' as nrTitle, \
			artikyl, \
			idprojekt, \
			UNIX_TIMESTAMP(data) as data, \
			if(price='' or price='0', 0, 1) as hasPrice, \
			if(price='' or price='0', 0, price) as price, \
			(select projekt.pay as pay from projekt where projekt.id = goods.idprojekt) as pay, \
			pay as goodsPay, \
			firmShowStatus as firmShowStatus, \
			1000 as countGoods, \
			1 as ind \
		from goods \
		where  id >= $start AND id <= $end \
		ORDER BY id ASC 
		
	sql_attr_uint 		= idprojekt
	sql_attr_uint 		= ind
	sql_attr_timestamp 	= data
	sql_attr_uint 		= hasPrice
	sql_attr_uint 		= price
	sql_attr_uint 		= pay
	sql_attr_uint 		= goodsPay
	sql_attr_uint 		= countGoods
	sql_attr_string		= firmShowStatus
	
}

source obuvua_nr : obuvua_base
{

	sql_query_pre 		= SET NAMES utf8
	sql_query_range 	= SELECT MIN(id), MAX(id) FROM `nr` 
	sql_range_step 		= 500
	
	sql_query = \
		select \
			id, \
			name_rus as nrTitle, \
			'' as name, \
			'' as keywords, \
			'' as about, \
			UNIX_TIMESTAMP(now()) as data, \
			1 as hasPrice, \
			0 as price, \
			1 as pay, \
			2 as ind, \
			cg as countgoods \
		from `nr` \
		where  id >= $start AND id <= $end \
		ORDER BY id ASC 
		

		
	sql_attr_uint 		= ind
	sql_attr_timestamp 	= data
	sql_attr_uint 		= hasPrice
	sql_attr_uint 		= price
	sql_attr_uint 		= pay
	sql_attr_uint 		= countGoods
}

source obuvua_goodscat : obuvua_base
{

	sql_query_pre 		= SET NAMES utf8
	sql_query_range 	= SELECT MIN(id), MAX(id) FROM goodscat
	sql_range_step 		= 200 
	
		
	sql_query = \
		select  \
			ttt.id,  \
			ttt.name,  \
			ttt.label as keywords,	 \
			UNIX_TIMESTAMP(now()) as `data`,  \
			'1' as `hasPrice`,   \
			'0' as `price`, \
			(select projekt.pay as pay from projekt where projekt.id = ttt.idprojekt) as pay,  \
			(select count(*) from goods where goods.rub = ttt.id or goods.parentRubId = ttt.id) as countGoods,  \
			ttt.idprojekt,   \
			3 as ind   \
		from goodscat as ttt \
		where ttt.id >= $start AND ttt.id <= $end  \
		ORDER BY id ASC 
		
	sql_attr_uint 		= ind
	sql_attr_timestamp 	= data
	sql_attr_uint 		= hasPrice
	sql_attr_uint 		= price
	sql_attr_uint 		= pay
	sql_attr_uint 		= countGoods
	sql_attr_uint 		= idprojekt
}


source obuvua_projekt : obuvua_base
{

	sql_query_pre 		= SET NAMES utf8
	sql_query_range 	= SELECT MIN(id), MAX(id) FROM `projekt` 
	sql_range_step 		= 500
	
	sql_query = \
		select \
			id, \
			name, \
			whoare, \
			searchworld, \
			pay, \
			city, \
			type, \
			showStatus, \
			5 as ind\
		from projekt \
		where id >= $start AND id <= $end \
		ORDER BY id ASC 
		
	sql_attr_uint 		= ind
	sql_attr_uint 		= pay
	sql_attr_uint 		= type
	sql_attr_uint 		= city
	sql_attr_string		= showStatus
	sql_attr_multi 		= uint aNrId from query; SELECT idprojekt, rub \
												FROM projekt_rub \
												union \
												SELECT projekt_rub.idprojekt, nr.parent as rub \
												FROM projekt_rub \
												LEFT JOIN nr on nr.id = projekt_rub.rub \
												where nr.parent != 0 
}

###---------- OBUVUA
###------------------------------------------------------------------------



###------------------------------------------------------------------------
###---------- BIGOPT

source goods : base
{

	sql_query_pre 		= SET NAMES utf8
	sql_query_range 	= SELECT MIN(id), MAX(id) FROM goods
	sql_range_step 		= 500
	
	sql_query = \
		select \
			id, \
			name, \
			keywords, \
			about, \
			'' as nrTitle, \
			artikyl, \
			idprojekt, \
			marketrub, \
			UNIX_TIMESTAMP(data) as data, \
			if(price='' or price='0', 0, 1) as hasPrice, \
			if(price='' or price='0', 0, price) as price, \
			dropPrice, \
			rrPrice, \
			(select projekt.pay as pay from projekt where projekt.id = goods.idprojekt) as pay, \
			pay as goodsPay, \
			firmPay, \
			firmShowStatus as firmShowStatus, \
			1000 as countGoods, \
			1 as ind \
		from goods \
		where  id >= $start AND id <= $end \
		ORDER BY id ASC 
		
	sql_attr_uint 		= idprojekt
	sql_attr_uint 		= ind
	sql_attr_timestamp 	= data
	sql_attr_uint 		= hasPrice
	sql_attr_uint 		= price
	sql_attr_uint 		= dropPrice
	sql_attr_uint 		= rrPrice
	sql_attr_uint 		= pay
	sql_attr_uint 		= goodsPay
	sql_attr_uint 		= countGoods
	sql_attr_uint 		= marketrub
	sql_attr_uint 		= firmPay
	sql_attr_string 	= firmShowStatus
	sql_attr_multi 		= uint aNrId from query; SELECT goods.id, nr.parent FROM goods LEFT JOIN nr on nr.id = goods.marketrub WHERE goods.marketrub != 0 AND nr.parent != 0 UNION SELECT goods.id, goods.marketrub FROM goods WHERE goods.marketrub != 0 
} 

source nr : base
{

	sql_query_pre 		= SET NAMES utf8
	sql_query_range 	= SELECT MIN(id), MAX(id) FROM `nr` 
	sql_range_step 		= 500
	
	sql_query = \
		select \
			id, \
			name_rus as nrTitle, \
			'' as name, \
			'' as keywords, \
			'' as about, \
			UNIX_TIMESTAMP(now()) as data, \
			1 as hasPrice, \
			0 as price, \
			1 as pay, \
			cg as countGoods, \
			'show' as firmShowStatus, \
			'publicated' as state, \
			2 as ind \
		from  \
			nr \
		where  \
			id >= $start AND  \
			id <= $end \
		ORDER BY id ASC 
		
#	sql_query = \
#		select  \
#			ttt.id, \
#			ttt.name as nrTitle, \
#			'' as name, \
#			'' as keywords, \
#			'' as about, \
#			UNIX_TIMESTAMP(now()) as data, \
#			1 as hasPrice,  \
#			0 as price,  \
#			1 as pay,  \
#			ttt.countGoods, \
#			'show' as firmShowStatus, \
#			2 as ind  \
#		from ( \
#			select \
#				nr2.id as id, \
#				nr2.name_rus as name, \
#				count(*) as countGoods \
#			from goods as g  \
#			left join nr on g.marketrub = nr.id \
#			left join nr as nr2 on nr.parent = nr2.id \
#			where nr2.id is not null and nr2.id != 0 \
#			group by nr2.id \
#			union \
#			select  \
#				nr.id as id,  \
#				nr.name_rus as name,  \
#				count(*) as countGoods \
#			from goods as g  \
#			left join nr on g.marketrub = nr.id \
#			left join nr as nr2 on nr.parent = nr2.id \
#			where nr.id is not null and nr2.id != 0\
#			group by nr.id \
#		) as ttt \
#		where id >= $start AND id <= $end \
#		ORDER BY id ASC  
		
	sql_attr_uint 		= ind
	sql_attr_timestamp 	= data
	sql_attr_uint 		= hasPrice
	sql_attr_uint 		= price
	sql_attr_uint 		= pay
	sql_attr_uint 		= countGoods
	sql_attr_string		= firmShowStatus
}

source goodscat : base
{

	sql_query_pre 		= SET NAMES utf8
	sql_query_range 	= SELECT MIN(id), MAX(id) FROM goodscat
	sql_range_step 		= 200
	
	sql_query = \
		select  \
			ttt.id,  \
			ttt.name,  \
			ttt.label as keywords,	 \
			UNIX_TIMESTAMP(now()) as `data`,  \
			'1' as `hasPrice`,   \
			'0' as `price`, \
			(select projekt.pay as pay from projekt where projekt.id = ttt.idprojekt) as pay,  \
			(select count(*) from goods left join goodscat on goods.rub = goodscat.id where goodscat.id = ttt.id or goodscat.parent = ttt.id) as countGoods,  \
			ttt.idprojekt,   \
			'show' as firmShowStatus, \
			3 as ind   \
		from goodscat as ttt \
		where ttt.id >= $start AND ttt.id <= $end \
		ORDER BY ttt.id ASC 
		
#	sql_query = \
#		select \
#			ttt.id, \
#			ttt.name, \
#			ttt.label as keywords,	\
#			UNIX_TIMESTAMP(now()) as data, \
#			1 as hasPrice,  \
#			0 as price,  \
#			(select projekt.pay as pay from projekt where projekt.id = ttt.idprojekt) as pay, \
#			ttt.countGoods, \
#			idprojekt as idprojekt,  \
#			'show' as firmShowStatus, \
#			3 as ind  \
#		from ( \
#			select  \
#				nr2.id as id,  \
#				nr2.name as name, \
#				nr2.label as label,\
#				count(*) as countGoods, \
#				nr2.idprojekt as idprojekt \
#			from goods as g  \
#			left join goodscat as nr on g.rub = nr.id \
#			left join goodscat as nr2 on nr.parent = nr2.id \
#			where nr2.id is not null and nr2.id != 0 \
#			group by nr2.id \
#			union \ 
#			select  \
#				nr.id as id,  \
#				nr.name as name, \
#				nr.label as label, \
#				count(*) as countGoods, \
#				nr.idprojekt as idprojekt \
#			from goods as g  \
#			left join goodscat as nr on g.rub = nr.id \
#			left join goodscat as nr2 on nr.parent = nr2.id \
#			where nr.id is not null and nr.id != 0 \
#			group by nr.id \
#		) as ttt \
#		where id >= $start AND id <= $end  \
#		ORDER BY id ASC 
		
	sql_attr_uint 		= ind
	sql_attr_timestamp 	= data
	sql_attr_uint 		= hasPrice
	sql_attr_uint 		= price
	sql_attr_uint 		= pay
	sql_attr_uint 		= countGoods
	sql_attr_uint 		= idprojekt
	sql_attr_string		= firmShowStatus
}


source informer : base
{

	sql_query_pre 		= SET NAMES utf8
	sql_query_range 	= SELECT MIN(id), MAX(id) FROM `informers` 
	sql_range_step 		= 500
	
	sql_query = \
		select \
			id, \
			name, \
			4 as ind\
		from informers \
		where status = 1 and id >= $start AND id <= $end \
		ORDER BY id ASC 
		
	sql_attr_uint 		= ind
}

source projekt : base
{

	sql_query_pre 		= SET NAMES utf8
	sql_query_range 	= SELECT MIN(id), MAX(id) FROM `projekt` 
	sql_range_step 		= 500
	
	sql_query = \
		select \
			id, \
			name, \
			whoare, \
			searchworld, \
			pay, \
			type, \
			city, \
			showStatus, \
			5 as ind\
		from projekt \
		where id >= $start AND id <= $end \
		ORDER BY id ASC 
		
	sql_attr_uint 		= ind
	sql_attr_uint 		= pay
	sql_attr_uint 		= type
	sql_attr_uint 		= city
	sql_attr_string		= showStatus
	sql_attr_multi 		= uint aNrId from query; SELECT idprojekt, rub \
												FROM projekt_rub \
												union \
												SELECT projekt_rub.idprojekt, nr.parent as rub \
												FROM projekt_rub \
												LEFT JOIN nr on nr.id = projekt_rub.rub \
												where nr.parent != 0 
	sql_attr_multi 		= uint aTagId from query; SELECT firmId, firmTagId \
												FROM firmTagList 
}

source wall : base
{ 

	sql_query_pre 		= SET NAMES utf8
	sql_query_range 	= SELECT MIN(id), MAX(id) FROM `wall`
	sql_range_step 		= 500
	
	sql_query = \
		select \
			id, \
			target, \
			firmId, \
			userId, \
			UNIX_TIMESTAMP(date) as date, \
			forMy, \
			text \
		from wall \
		where id >= $start AND id <= $end \
		ORDER BY id ASC 
		
	sql_field_string	= target
	sql_attr_uint 		= firmId
	sql_attr_uint 		= userId
	sql_attr_timestamp	= date
	sql_attr_uint 		= forMy
}

###---------- BIGOPT
###------------------------------------------------------------------------


###------------------------------------------------------------------------
###---------- AGROBIZ 

source ag_goods : ag_base
{

	sql_query_pre 		= SET NAMES utf8
	sql_query_range 	= SELECT MIN(id), MAX(id) FROM goods
	sql_range_step 		= 500
	
	sql_query = \
		select \
			id, \
			name, \
			keywords, \
			about, \
			artikyl, \
			'' as nrTitle, \
			idprojekt, \
			marketrub, \
			rub, \
			UNIX_TIMESTAMP(data) as data, \
			if(price='' or price='0', 0, 1) as hasPrice, \
			if(price='' or price='0', 0, price) as price, \
			(select projekt.pay as pay from projekt where projekt.id = goods.idprojekt) as pay, \
			1000 as countGoods, \
			pay as goodsPay, \
			firmShowStatus, \
			state as state, \
			priceConvert, \
			1 as ind \
		from goods \
		where  id >= $start AND id <= $end \
		ORDER BY id ASC 
		
	sql_attr_uint 		= idprojekt
	sql_attr_uint 		= ind
	sql_attr_timestamp 	= data
	sql_attr_uint 		= hasPrice
	sql_attr_uint 		= price
	sql_attr_uint 		= pay
	sql_attr_uint 		= marketrub
	sql_attr_uint 		= rub
	sql_attr_uint 		= goodsPay
	sql_attr_uint 		= countGoods
	sql_attr_float 		= priceConvert
	sql_attr_string 		= firmShowStatus
	sql_attr_string 		= state
}

source ag_nr : ag_base
{

	sql_query_pre 		= SET NAMES utf8
	sql_query_range 	= SELECT MIN(id), MAX(id) FROM `nr` 
	sql_range_step 		= 500
	
	
	sql_query = \
		select \
			id, \
			name_rus as nrTitle, \
			'' as name, \
			'' as keywords, \
			'' as about, \
			UNIX_TIMESTAMP(now()) as data, \
			1 as hasPrice, \
			0 as price, \
			1 as pay, \
			cg as countGoods, \
			'show' as firmShowStatus, \
			'publicated' as state, \
			2 as ind \
		from  \
			nr \
		where  \
			id >= $start AND  \
			id <= $end \
		ORDER BY id ASC 

	
#	sql_query = \
#		select  \
#			ttt.id, \
#			ttt.name as nrTitle, \
#			'' as name, \
#			'' as keywords, \
#			'' as about, \
#			UNIX_TIMESTAMP(now()) as data, \
#			1 as hasPrice,  \
#			0 as price,  \
#			1 as pay,  \
#			ttt.countGoods, \
#			'show' as firmShowStatus, \
#			'publicated' as state, \
#			2 as ind  \
#		from ( \
#			select \
#				nr2.id as id, \
#				nr2.name_rus as name, \
#				count(*) as countGoods \
#			from goods as g  \
#			left join nr on g.marketrub = nr.id \
#			left join nr as nr2 on nr.parent = nr2.id \
#			where nr2.id is not null and nr2.id != 0 \
#			group by nr2.id \
#			union \
#			select  \
#				nr.id as id,  \
#				nr.name_rus as name,  \
#				count(*) as countGoods \
#			from goods as g  \
#			left join nr on g.marketrub = nr.id \
#			left join nr as nr2 on nr.parent = nr2.id \
#			where nr.id is not null and nr2.id != 0\
#			group by nr.id \
#		) as ttt \
#		where id >= $start AND id <= $end \
#		ORDER BY id ASC 
		
	sql_attr_uint 		= ind
	sql_attr_timestamp 	= data
	sql_attr_uint 		= hasPrice
	sql_attr_uint 		= price
	sql_attr_uint 		= pay
	sql_attr_uint 		= countGoods
	sql_attr_string 		= firmShowStatus
	sql_attr_string 		= state
}

source ag_goodscat : ag_base
{

	sql_query_pre 		= SET NAMES utf8
	sql_query_range 	= SELECT MIN(id), MAX(id) FROM goodscat
	sql_range_step 		= 500
	
	sql_query = \
		select \
			ttt.id, \
			ttt.name, \
			ttt.label as keywords,	\
			UNIX_TIMESTAMP(now()) as data, \
			1 as hasPrice,  \
			0 as price, \
			(select projekt.pay as pay from projekt where projekt.id = ttt.idprojekt) as pay, \
			(select projekt.showStatus as showStatus from projekt where projekt.id = ttt.idprojekt) as firmShowStatus, \
			(select count(*) from goodsCategoryAll where goodsCategoryAll.rubId = ttt.id ) as countGoods, \
			ttt.idprojekt,  \
			3 as ind  \
		from goodscat as ttt \
		where ttt.id >= $start AND ttt.id <= $end  \
		ORDER BY id ASC 
		 
#	sql_query = \
#		select \
#			ttt.id, \
#			ttt.name, \
#			ttt.label as keywords,	\
#			UNIX_TIMESTAMP(now()) as data, \
#			1 as hasPrice,  \
#			0 as price,  \
#			(select projekt.pay as pay from projekt where projekt.id = ttt.idprojekt) as pay, \
#			(select projekt.showStatus as showStatus from projekt where projekt.id = ttt.idprojekt) as firmShowStatus, \
#			ttt.countGoods, \
#			idprojekt as idprojekt,  \
#			3 as ind  \
#		from ( \
#			select  \
#				nr2.id as id,  \
#				nr2.name as name, \
#				nr2.label as label,\
#				count(*) as countGoods, \
#				nr2.idprojekt as idprojekt \
#			from goods as g  \
#			left join goodscat as nr on g.rub = nr.id \
#			left join goodscat as nr2 on nr.parent = nr2.id \
#			where nr2.id is not null and nr2.id != 0 \
#			group by nr2.id \
#			union \
#			select  \
#				nr.id as id,  \
#				nr.name as name, \
#				nr.label as label, \
#				count(*) as countGoods, \
#				nr.idprojekt as idprojekt \
#			from goods as g  \
#			left join goodscat as nr on g.rub = nr.id \
#			left join goodscat as nr2 on nr.parent = nr2.id \
#			where nr.id is not null and nr.id != 0 \
#			group by nr.id \
#		) as ttt \
#		where id >= $start AND id <= $end  \
#		ORDER BY id ASC 
		
	sql_attr_uint 		= ind
	sql_attr_timestamp 	= data
	sql_attr_uint 		= hasPrice
	sql_attr_uint 		= price
	sql_attr_uint 		= pay
	sql_attr_uint 		= countGoods
	sql_attr_uint 		= idprojekt
	sql_attr_string 		= firmShowStatus
}


source ag_informer : ag_base
{

	sql_query_pre 		= SET NAMES utf8
	sql_query_range 	= SELECT MIN(id), MAX(id) FROM `informers` 
	sql_range_step 		= 500
	
	sql_query = \
		select \
			id, \
			name, \
			keywords, \
			4 as ind \
		from informers \
		where status = 1 and id >= $start AND id <= $end \
		ORDER BY id ASC 
		
	sql_attr_uint 		= ind
}

source ag_projekt : ag_base
{

	sql_query_pre 		= SET NAMES utf8
	sql_query_range 	= SELECT MIN(id), MAX(id) FROM `projekt` 
	sql_range_step 		= 500
	
	sql_query = \
		select \
			id, \
			name, \
			whoare, \
			searchworld, \
			pay, \
			type, \
			showStatus, \
			5 as ind\
		from projekt \
		where id >= $start AND id <= $end \
		ORDER BY id ASC 
		
	sql_attr_uint 		= ind
	sql_attr_uint 		= pay
	sql_attr_string 		= showStatus
	sql_attr_uint 		= type
	sql_attr_multi 		= uint aNrId from query; SELECT idprojekt, rub \
												FROM projekt_rub \
												union \
												SELECT projekt_rub.idprojekt, nr.parent as rub \
												FROM projekt_rub \
												LEFT JOIN nr on nr.id = projekt_rub.rub \
												where nr.parent != 0 
}

source ag_text : ag_base
{

	sql_query_pre 		= SET NAMES utf8
	sql_query_range 	= SELECT MIN(id), MAX(id) FROM `text` 
	sql_range_step 		= 500
	
	sql_query = \
		select \
			id, \
			name, \
			text, \
			expertId, \
			marketrub, \
			idprojekt, \
			status, \
			UNIX_TIMESTAMP(concat(`data`, ' ', `time`)) as `date`, \
			(SELECT group_concat(distinct(label) SEPARATOR ', ') FROM `mitku` where m = 8 and idm = `text`.id) as labels, \
			6 as ind\
		from `text` \
		where id >= $start AND id <= $end \
		ORDER BY id ASC 
	
	sql_attr_uint 		= expertId	
	sql_attr_uint 		= marketrub
	sql_attr_uint 		= status
	sql_attr_uint 		= idprojekt
	sql_attr_uint 		= ind
	sql_attr_timestamp 	= date 
	
}

source ag_advert : ag_base
{

	sql_query_pre 		= SET NAMES utf8
	sql_query_range 	= SELECT MIN(id), MAX(id) FROM `advert` 
	sql_range_step 		= 500
	
	sql_query = \
		select \
			id, \
			title, \
			des, \
			nrId, \
			type, \
			status, \
			idprojekt, \
			UNIX_TIMESTAMP(`date`) as `date`, \
			7 as ind\
		from `advert` \
		where id >= $start AND id <= $end \
		ORDER BY id ASC 
	
	sql_attr_uint 		= nrId	
	sql_attr_uint 		= type
	sql_attr_uint 		= status
	sql_attr_uint 		= idprojekt
	sql_attr_uint 		= ind
	sql_attr_timestamp 	= date 
	
}

###---------- AGROBIZ
###------------------------------------------------------------------------

###------------------------------------------------------------------------
###---------- PODKAPOT

source kapot_filter : kapot_base
{

	sql_query_pre 		= SET NAMES utf8
	sql_query_range 	= SELECT MIN(id), MAX(id) FROM filter
	sql_range_step 		= 500
	
	sql_query = \
		select \
			filter.id, \
			'' as fi, \
			filter.idprojekt, \
			filter.yearFrom, \
			filter.yearTo, \
			if( filter.condition = 'new', 1, if(filter.condition = 'used', 2, 0) ) as conditionInt, \
			filter.isAllMake, \
			filter.isAllModel, \
			filter.isAllNr, \
			filter.isAllSubNr, \
			filter.isAllRegion, \
			filter.isAllCity, \
			projekt.pay as firmPay, \
			projekt.regionId as firmRegionId, \
			projekt.city as firmCityId, \
			if(filter.isAllMake = 1, (select group_concat(id) from marka where parent = 0), (select group_concat(filterMake.makeId) from filterMake where filterMake.filterId = filter.id)) as allMake, \
			if(filter.isAllModel = 1, (select group_concat(id) from marka where parent != 0), (select group_concat(filterModel.modelId) from filterModel where filterModel.filterId = filter.id)) as allModel, \
			if(filter.isAllNr = 1, (select group_concat(id) from nr where parent = 0), (select group_concat(filterNr.nrId) from filterNr where filterNr.filterId = filter.id)) as allNr, \
			if(filter.isAllSubNr = 1, (select group_concat(id) from nr where parent != 0), (select group_concat(filterSubNr.subNrId) from filterSubNr where filterSubNr.filterId = filter.id)) as allSubNr, \
			if(filter.isAllRegion = 1, (select group_concat(id) from goroda where parent = 0), (select group_concat(filterRegion.regionId) from filterRegion where filterRegion.filterId = filter.id)) as allRegion, \
			if(filter.isAllCity = 1, (select group_concat(id) from goroda where parent != 0), (select group_concat(filterCity.cityId) from filterCity where filterCity.filterId = filter.id)) as allCity, \
			6 as ind \
		from filter \
		left join projekt on projekt.id = filter.idprojekt \
		where  filter.id >= $start AND filter.id <= $end \
		ORDER BY filter.id ASC 
		
	sql_attr_uint 		= idprojekt
	sql_attr_uint 		= ind
	sql_attr_uint 		= yearFrom
	sql_attr_uint 		= yearTo
	sql_attr_uint 		= conditionInt
	sql_attr_uint 		= isAllMake
	sql_attr_uint 		= isAllModel
	sql_attr_uint 		= isAllNr
	sql_attr_uint 		= isAllSubNr
	sql_attr_uint 		= isAllRegion
	sql_attr_uint 		= isAllCity
	sql_attr_uint 		= firmPay
	sql_attr_uint 		= firmRegionId
	sql_attr_uint 		= firmCityId
	sql_attr_multi 		= uint allMake from field
	sql_attr_multi 		= uint allModel from field
	sql_attr_multi 		= uint allNr from field
	sql_attr_multi 		= uint allSubNr from field
	sql_attr_multi 		= uint allRegion from field
	sql_attr_multi 		= uint allCity from field
}

source kapot_goods : kapot_base
{

	sql_query_pre 		= SET NAMES utf8
	sql_query_range 	= SELECT MIN(id), MAX(id) FROM goods
	sql_range_step 		= 500
	
	sql_query = \
		select \
			goods.id, \
			goods.artikyl, \
			goods.name, \
			goods.keywords, \
			goods.about, \
			goods.idprojekt, \
			UNIX_TIMESTAMP(data) as data, \
			if(price='' or price='0', 0, 1) as hasPrice, \
			if(price='' or price='0', 0, price) as price, \
			projekt.pay as pay, \
			projekt.city as cityId, \
			projekt.regionId as regionId, \
			goods.pay as goodsPay, \
			if( `condition` = 'new', 1, if( `condition` = 'used', 2, 1) ) as conditionInt, \
			marketrub, \
			nr.parent as marketrubParent, \
			projekt.pay as firmPay, \
			1000 as countGoods, \
			goods.nrSort, \
			if(nr.parent is not null, concat(nr.parent, ',', marketrub), '') as allMarketrub, \
			if(projekt.city > 0 and projekt.regionId is not null, concat(projekt.regionId, ',', projekt.city), '') as allRegion, \
			1 as ind \
		from goods \
			left join nr on nr.id = goods.marketrub \
			left join projekt on projekt.id = goods.idprojekt \
		where goods.idprojekt = 24406 and goods.id >= $start AND goods.id <= $end \
		ORDER BY goods.id ASC 
		
	sql_attr_uint 		= idprojekt
	sql_attr_uint 		= ind
	sql_attr_timestamp 	= data
	sql_attr_uint 		= hasPrice
	sql_attr_uint 		= price
	sql_attr_uint 		= pay
	sql_attr_uint 		= goodsPay
	sql_attr_uint 		= countGoods
	sql_attr_uint 		= conditionInt
	sql_attr_uint 		= marketrub
	sql_attr_uint 		= marketrubParent
	sql_attr_uint 		= firmPay
	sql_attr_uint 		= cityId
	sql_attr_uint 		= regionId
	sql_attr_uint 		= nrSort
	sql_attr_multi 		= uint allRegion from field
	sql_attr_multi 		= uint allMarketrub from field
	sql_attr_multi 		= uint modelId from query; SELECT goodsId, modelId FROM goodsModel
	sql_attr_multi 		= uint makeId from query; SELECT goodsId, makeId FROM goodsModel
}

source kapot_nr : kapot_base
{

	sql_query_pre 		= SET NAMES utf8
	sql_query_range 	= SELECT MIN(id), MAX(id) FROM `nr` 
	sql_range_step 		= 500
	
	sql_query = \
		select \
			id, \
			name_rus as nrTitle, \
			'' as name, \
			'' as keywords, \
			'' as about, \
			UNIX_TIMESTAMP(now()) as data, \
			1 as hasPrice, \
			0 as price, \
			1 as pay, \
			cg as countGoods, \
			'show' as firmShowStatus, \
			'publicated' as state, \
			2 as ind \
		from  \
			nr \
		where  \
			id >= $start AND  \
			id <= $end \
		ORDER BY id ASC 
		
#	sql_query = \
#		select  \
#			ttt.id, \
#			ttt.name, \
#			UNIX_TIMESTAMP(now()) as data, \
#			1 as hasPrice,  \
#			0 as price,  \
#			1 as pay,  \
#			ttt.countGoods, \
#			2 as ind  \
#		from ( \
#			select \
#				nr2.id as id, \
#				nr2.name_rus as name, \
#				count(*) as countGoods \
#			from goods as g  \
#			left join nr on g.marketrub = nr.id \
#			left join nr as nr2 on nr.parent = nr2.id \
#			where nr2.id is not null and nr2.id != 0 \
#			group by nr2.id \
#			union \
#			select  \
##				nr.id as id,  \
#				nr.name_rus as name,  \
#				count(*) as countGoods \
#			from goods as g  \
#			left join nr on g.marketrub = nr.id \
#			left join nr as nr2 on nr.parent = nr2.id \
#			where nr.id is not null and nr2.id != 0\
#			group by nr.id \
#		) as ttt \
#		where id >= $start AND id <= $end \
#		ORDER BY id ASC 
		
	sql_attr_uint 		= ind
	sql_attr_timestamp 	= data
	sql_attr_uint 		= hasPrice
	sql_attr_uint 		= price
	sql_attr_uint 		= pay
	sql_attr_uint 		= countGoods
}

source kapot_goodscat : kapot_base
{

	sql_query_pre 		= SET NAMES utf8
	sql_query_range 	= SELECT MIN(id), MAX(id) FROM goodscat
	sql_range_step 		= 500
	
	sql_query = \
		select \
			ttt.id, \
			ttt.name, \
			ttt.label as keywords,	\
			UNIX_TIMESTAMP(now()) as data, \
			1 as hasPrice,  \
			0 as price, \
			(select projekt.pay as pay from projekt where projekt.id = ttt.idprojekt) as pay, \
			(select projekt.showStatus as showStatus from projekt where projekt.id = ttt.idprojekt) as firmShowStatus, \
			(select count(*) from goodsCategoryAll where goodsCategoryAll.rubId = ttt.id ) as countGoods, \
			ttt.idprojekt,  \
			3 as ind  \
		from goodscat as ttt \
		where ttt.id >= $start AND ttt.id <= $end  \
		ORDER BY id ASC 
		
#	sql_query = \
#		select \
#			ttt.id, \
#			ttt.name, \
#			ttt.label as keywords,	\
#			UNIX_TIMESTAMP(now()) as data, \
#			1 as hasPrice,  \
#			0 as price,  \
#			(select projekt.pay as pay from projekt where projekt.id = ttt.idprojekt) as pay, \
#			ttt.countGoods, \
#			idprojekt as idprojekt,  \
#			3 as ind  \
#		from ( \
#			select  \
#				nr2.id as id,  \
#				nr2.name as name, \
#				nr2.label as label,\
#				count(*) as countGoods, \
#				nr2.idprojekt as idprojekt \
#			from goods as g  \
#			left join goodscat as nr on g.rub = nr.id \
#			left join goodscat as nr2 on nr.parent = nr2.id \
#			where nr2.id is not null and nr2.id != 0 \
#			group by nr2.id \
#			union \
#			select  \
#				nr.id as id,  \
#				nr.name as name, \
#				nr.label as label, \
#				count(*) as countGoods, \
#				nr.idprojekt as idprojekt \
#			from goods as g  \
#			left join goodscat as nr on g.rub = nr.id \
#			left join goodscat as nr2 on nr.parent = nr2.id \
#			where nr.id is not null and nr.id != 0 \
#			group by nr.id \
#		) as ttt \
#		where id >= $start AND id <= $end  \
#		ORDER BY id ASC 
		
	sql_attr_uint 		= ind
	sql_attr_timestamp 	= data
	sql_attr_uint 		= hasPrice
	sql_attr_uint 		= price
	sql_attr_uint 		= pay
	sql_attr_uint 		= countGoods
	sql_attr_uint 		= idprojekt
}

source kapot_price : kapot_base
{

	sql_query_pre 		= SET NAMES utf8
	sql_query_range 	= SELECT MIN(id), MAX(id) FROM `price` 
	sql_range_step 		= 500
	
	sql_query = \
		select \
			price.id, \
			price.kod, \
			price.name, \
			1 as hasPrice, \
			0 as price, \
			(select projekt.pay as pay from projekt where projekt.id = price.idprojekt) as pay, \
			10 as countGoods, \
			4 as ind \
		from price \
		where id >= $start AND id <= $end \
		ORDER BY id ASC 
		
	sql_attr_uint 		= ind
	sql_attr_uint 		= hasPrice
	sql_attr_uint 		= price
	sql_attr_uint 		= pay
	sql_attr_uint 		= countGoods
}

#source kapot_informer : kapot_base
#{
#
#	sql_query_pre 		= SET NAMES utf8
#	sql_query_range 	= SELECT MIN(id), MAX(id) FROM `informers` 
#	sql_range_step 		= 500
#	
#	sql_query = \
#		select \
#			id, \
#			name, \
#			4 as ind\
#		from informers \
#		where status = 1 and looklimit > look and id >= $start AND id <= $end \
#		ORDER BY id ASC 
#		
#	sql_attr_uint 		= ind
#}

source kapot_projekt : kapot_base
{

	sql_query_pre 		= SET NAMES utf8
	sql_query_range 	= SELECT MIN(id), MAX(id) FROM `projekt` 
	sql_range_step 		= 500
	
	sql_query = \
		select \
			id, \
			name, \
			whoare, \
			searchworld, \
			pay, \
			power, \
			type, \
			city as cityId, \
			regionId, \
			if(city > 0 and regionId is not null, concat(regionId, ',', city), '') as allRegion, \
			5 as ind\
		from projekt \
		where id >= $start AND id <= $end \
		ORDER BY id ASC 
		
	sql_attr_uint 		= ind
	sql_attr_uint 		= pay
	sql_attr_uint 		= power
	sql_attr_uint 		= type
	sql_attr_uint 		= cityId
	sql_attr_uint 		= regionId
	sql_attr_multi 		= uint allRegion from field
	
	sql_attr_multi 		= uint nrId from query; SELECT idprojekt, rub as nrId FROM projekt_rub
	sql_attr_multi 		= uint parentNrId from query; SELECT dd.idprojekt, nr.parent as parentNrId FROM projekt_rub  as dd left join nr on nr.id = dd.`rub` where nr.parent != 0 and nr.parent is not null
	
	sql_attr_multi 		= uint allNrId from query; SELECT idprojekt, rub as allNrId FROM projekt_rub union SELECT idprojekt, nr.parent as allNrId FROM projekt_rub as dd left join nr on nr.id = dd.`rub` where nr.parent != 0 and nr.parent is not null
	
	sql_attr_multi 		= uint modelId from query; SELECT idprojekt, rub as modelId FROM projekt_marka
	sql_attr_multi 		= uint makeId from query; SELECT idprojekt, marka.parent as makeId FROM projekt_marka left join marka on marka.id = projekt_marka.rub where marka.parent != 0 and marka.parent is not null
}

###---------- PODKAPOT
###------------------------------------------------------------------------

###------------------------------------------------------------------------
###---------- NAVOLYNI

source navolyni_goods : navolyni_base
{

	sql_query_pre 		= SET NAMES utf8
	sql_query_range 	= SELECT MIN(id), MAX(id) FROM goods
	sql_range_step 		= 500
	
	sql_query = \
		select \
			id, \
			name, \
			keywords, \
			about, \
			artikyl, \
			'' as nrTitle, \
			idprojekt, \
			UNIX_TIMESTAMP(data) as data, \
			if(price='' or price='0', 0, 1) as hasPrice, \
			if(price='' or price='0', 0, price) as price, \
			(select projekt.pay as pay from projekt where projekt.id = goods.idprojekt) as pay, \
			1000 as countGoods, \
			pay as goodsPay, \
			firmShowStatus as firmShowStatus, \
			1 as ind \
		from goods \
		where  id >= $start AND id <= $end \
		ORDER BY id ASC 
		
	sql_attr_uint 		= idprojekt
	sql_attr_uint 		= ind
	sql_attr_timestamp 	= data
	sql_attr_uint 		= hasPrice
	sql_attr_uint 		= price
	sql_attr_uint 		= pay
	sql_attr_uint 		= goodsPay
	sql_attr_uint 		= countGoods
	sql_attr_string		= firmShowStatus
}

source navolyni_nr : navolyni_base
{

	sql_query_pre 		= SET NAMES utf8
	sql_query_range 	= SELECT MIN(id), MAX(id) FROM `nr` 
	sql_range_step 		= 500
	
#	sql_query = \
#		select \
#			id, \
#			name_rus, \
#			UNIX_TIMESTAMP(now()) as data, \
#			1 as hasPrice, \
#			0 as price, \
#			1 as pay, \
#			2 as ind \
#		from `nr` \
#		where  id >= $start AND id <= $end \
#		ORDER BY id ASC 
		
	sql_query = \
		select  \
			ttt.id, \
			ttt.name as nrTitle, \
			'' as name, \
			'' as keywords, \
			'' as about, \
			UNIX_TIMESTAMP(now()) as data, \
			1 as hasPrice,  \
			0 as price,  \
			1 as pay,  \
			ttt.countGoods, \
			2 as ind  \
		from ( \
			select \
				nr2.id as id, \
				nr2.name_rus as name, \
				count(*) as countGoods \
			from goods as g  \
			left join nr on g.marketrub = nr.id \
			left join nr as nr2 on nr.parent = nr2.id \
			where nr2.id is not null and nr2.id != 0 \
			group by nr2.id \
			union \
			select  \
				nr.id as id,  \
				nr.name_rus as name,  \
				count(*) as countGoods \
			from goods as g  \
			left join nr on g.marketrub = nr.id \
			left join nr as nr2 on nr.parent = nr2.id \
			where nr.id is not null and nr2.id != 0\
			group by nr.id \
		) as ttt \
		where id >= $start AND id <= $end \
		ORDER BY id ASC 
		
	sql_attr_uint 		= ind
	sql_attr_timestamp 	= data
	sql_attr_uint 		= hasPrice
	sql_attr_uint 		= price
	sql_attr_uint 		= pay
	sql_attr_uint 		= countGoods
}

source navolyni_goodscat : navolyni_base
{

	sql_query_pre 		= SET NAMES utf8
	sql_query_range 	= SELECT MIN(id), MAX(id) FROM goodscat
	sql_range_step 		= 500
	
#	sql_query = \
#		select \
#			id, \
#			name as goodsCatName, \
#			UNIX_TIMESTAMP(now()) as data, \
#			1 as hasPrice, \
#			0 as price, \
#			(select if(projekt.pay > 0, 1, 0) as pay from projekt where projekt.id = goodscat.idprojekt) as pay, \
#			3 as ind \
#		from goodscat \
#		where  id >= $start AND id <= $end \
#		ORDER BY id ASC 
		
	sql_query = \
		select \
			ttt.id, \
			ttt.name, \
			ttt.label as keywords,	\
			UNIX_TIMESTAMP(now()) as data, \
			1 as hasPrice,  \
			0 as price,  \
			(select projekt.pay as pay from projekt where projekt.id = ttt.idprojekt) as pay, \
			ttt.countGoods, \
			idprojekt as idprojekt,  \
			3 as ind  \
		from ( \
			select  \
				nr2.id as id,  \
				nr2.name as name, \
				nr2.label as label,\
				count(*) as countGoods, \
				nr2.idprojekt as idprojekt \
			from goods as g  \
			left join goodscat as nr on g.rub = nr.id \
			left join goodscat as nr2 on nr.parent = nr2.id \
			where nr2.id is not null and nr2.id != 0 \
			group by nr2.id \
			union \
			select  \
				nr.id as id,  \
				nr.name as name, \
				nr.label as label, \
				count(*) as countGoods, \
				nr.idprojekt as idprojekt \
			from goods as g  \
			left join goodscat as nr on g.rub = nr.id \
			left join goodscat as nr2 on nr.parent = nr2.id \
			where nr.id is not null and nr.id != 0 \
			group by nr.id \
		) as ttt \
		where id >= $start AND id <= $end  \
		ORDER BY id ASC 
		
	sql_attr_uint 		= ind
	sql_attr_timestamp 	= data
	sql_attr_uint 		= hasPrice
	sql_attr_uint 		= price
	sql_attr_uint 		= pay
	sql_attr_uint 		= countGoods
	sql_attr_uint 		= idprojekt
}


source navolyni_informer : navolyni_base
{

	sql_query_pre 		= SET NAMES utf8
	sql_query_range 	= SELECT MIN(id), MAX(id) FROM `informers` 
	sql_range_step 		= 500
	
	sql_query = \
		select \
			id, \
			name, \
			4 as ind\
		from informers \
		where status = 1 and id >= $start AND id <= $end \
		ORDER BY id ASC 
		
	sql_attr_uint 		= ind
}

source navolyni_projekt : navolyni_base
{

	sql_query_pre 		= SET NAMES utf8
	sql_query_range 	= SELECT MIN(id), MAX(id) FROM `projekt` 
	sql_range_step 		= 500
	
	sql_query = \
		select \
			id, \
			name, \
			whoare, \
			searchworld, \
			pay, \
			type, \
			showStatus, \
			5 as ind\
		from projekt \
		where id >= $start AND id <= $end \
		ORDER BY id ASC 
		
	sql_attr_uint 		= ind
	sql_attr_uint 		= pay
	sql_attr_uint 		= type
	sql_attr_string		= showStatus
}

source navolyni_tender : navolyni_base
{

	sql_query_pre 		= SET NAMES utf8
	sql_query_range 	= SELECT MIN(id), MAX(id) FROM `tender` 
	sql_range_step 		= 500
	
	sql_query = \
		select \
			id, \
			title, \
			if(type='buy', 1, 2) as type, \
			nrId, \
			des, \
			idprojekt, \
			if(status='wait', 1, 2) as status, \
			UNIX_TIMESTAMP(`date`) as `date`\
		from tender \
		where id >= $start AND id <= $end \
		ORDER BY id ASC 
	
	sql_attr_uint 		= type	
	sql_attr_uint 		= nrId
	sql_attr_uint 		= status
	sql_attr_uint 		= idprojekt
	sql_attr_timestamp 	= date 
	
}

###---------- NAVOLYNI
###------------------------------------------------------------------------

###------------------------------------------------------------------------
###---------- LVIVMARKET

source lvivmarket_goods : lvivmarket_base
{

	sql_query_pre 		= SET NAMES utf8
	sql_query_range 	= SELECT MIN(id), MAX(id) FROM goods
	sql_range_step 		= 500
	
	sql_query = \
		select \
			id, \
			name, \
			keywords, \
			about, \
			artikyl, \
			'' as nrTitle, \
			idprojekt, \
			UNIX_TIMESTAMP(data) as data, \
			if(price='' or price='0', 0, 1) as hasPrice, \
			if(price='' or price='0', 0, price) as price, \
			(select projekt.pay as pay from projekt where projekt.id = goods.idprojekt) as pay, \
			1000 as countGoods, \
			pay as goodsPay, \
			firmShowStatus as firmShowStatus, \
			1 as ind \
		from goods \
		where  id >= $start AND id <= $end \
		ORDER BY id ASC 
		
	sql_attr_uint 		= idprojekt
	sql_attr_uint 		= ind
	sql_attr_timestamp 	= data
	sql_attr_uint 		= hasPrice
	sql_attr_uint 		= price
	sql_attr_uint 		= pay
	sql_attr_uint 		= goodsPay
	sql_attr_uint 		= countGoods
	sql_attr_string		= firmShowStatus
}

source lvivmarket_nr : lvivmarket_base
{

	sql_query_pre 		= SET NAMES utf8
	sql_query_range 	= SELECT MIN(id), MAX(id) FROM `nr` 
	sql_range_step 		= 500
	
#	sql_query = \
#		select \
#			id, \
#			name_rus, \
#			UNIX_TIMESTAMP(now()) as data, \
#			1 as hasPrice, \
#			0 as price, \
#			1 as pay, \
#			2 as ind \
#		from `nr` \
#		where  id >= $start AND id <= $end \
#		ORDER BY id ASC 
		
	sql_query = \
		select  \
			ttt.id, \
			ttt.name as nrTitle, \
			'' as name, \
			'' as keywords, \
			'' as about, \
			UNIX_TIMESTAMP(now()) as data, \
			1 as hasPrice,  \
			0 as price,  \
			1 as pay,  \
			ttt.countGoods, \
			2 as ind, \
			'show' as firmShowStatus \
		from ( \
			select \
				nr2.id as id, \
				nr2.name_rus as name, \
				count(*) as countGoods \
			from goods as g  \
			left join nr on g.marketrub = nr.id \
			left join nr as nr2 on nr.parent = nr2.id \
			where nr2.id is not null and nr2.id != 0 \
			group by nr2.id \
			union \
			select  \
				nr.id as id,  \
				nr.name_rus as name,  \
				count(*) as countGoods \
			from goods as g  \
			left join nr on g.marketrub = nr.id \
			left join nr as nr2 on nr.parent = nr2.id \
			where nr.id is not null and nr2.id != 0\
			group by nr.id \
		) as ttt \
		where id >= $start AND id <= $end \
		ORDER BY id ASC 
		
	sql_attr_uint 		= ind
	sql_attr_timestamp 	= data
	sql_attr_uint 		= hasPrice
	sql_attr_uint 		= price
	sql_attr_uint 		= pay
	sql_attr_uint 		= countGoods
	sql_attr_string		= firmShowStatus
}

source lvivmarket_goodscat : lvivmarket_base
{

	sql_query_pre 		= SET NAMES utf8
	sql_query_range 	= SELECT MIN(id), MAX(id) FROM goodscat
	sql_range_step 		= 500
	
#	sql_query = \
#		select \
#			id, \
#			name as goodsCatName, \
#			UNIX_TIMESTAMP(now()) as data, \
#			1 as hasPrice, \
#			0 as price, \
#			(select if(projekt.pay > 0, 1, 0) as pay from projekt where projekt.id = goodscat.idprojekt) as pay, \
#			3 as ind \
#		from goodscat \
#		where  id >= $start AND id <= $end \
#		ORDER BY id ASC 
		
	sql_query = \
		select \
			ttt.id, \
			ttt.name, \
			ttt.label as keywords,	\
			UNIX_TIMESTAMP(now()) as data, \
			1 as hasPrice,  \
			0 as price,  \
			(select projekt.pay as pay from projekt where projekt.id = ttt.idprojekt) as pay, \
			ttt.countGoods, \
			idprojekt as idprojekt,  \
			3 as ind,  \
			'show' as firmShowStatus \
		from ( \
			select  \
				nr2.id as id,  \
				nr2.name as name, \
				nr2.label as label,\
				count(*) as countGoods, \
				nr2.idprojekt as idprojekt \
			from goods as g  \
			left join goodscat as nr on g.rub = nr.id \
			left join goodscat as nr2 on nr.parent = nr2.id \
			where nr2.id is not null and nr2.id != 0 \
			group by nr2.id \
			union \
			select  \
				nr.id as id,  \
				nr.name as name, \
				nr.label as label, \
				count(*) as countGoods, \
				nr.idprojekt as idprojekt \
			from goods as g  \
			left join goodscat as nr on g.rub = nr.id \
			left join goodscat as nr2 on nr.parent = nr2.id \
			where nr.id is not null and nr.id != 0 \
			group by nr.id \
		) as ttt \
		where id >= $start AND id <= $end  \
		ORDER BY id ASC 
		
	sql_attr_uint 		= ind
	sql_attr_timestamp 	= data
	sql_attr_uint 		= hasPrice
	sql_attr_uint 		= price
	sql_attr_uint 		= pay
	sql_attr_uint 		= countGoods
	sql_attr_uint 		= idprojekt
}


source lvivmarket_informer : lvivmarket_base
{

	sql_query_pre 		= SET NAMES utf8
	sql_query_range 	= SELECT MIN(id), MAX(id) FROM `informers` 
	sql_range_step 		= 500
	
	sql_query = \
		select \
			id, \
			name, \
			4 as ind\
		from informers \
		where status = 1 and id >= $start AND id <= $end \
		ORDER BY id ASC 
		
	sql_attr_uint 		= ind
}

source lvivmarket_projekt : lvivmarket_base
{

	sql_query_pre 		= SET NAMES utf8
	sql_query_range 	= SELECT MIN(id), MAX(id) FROM `projekt` 
	sql_range_step 		= 500
	
	sql_query = \
		select \
			id, \
			name, \
			whoare, \
			searchworld, \
			pay, \
			type, \
			showStatus, \
			5 as ind\
		from projekt \
		where id >= $start AND id <= $end \
		ORDER BY id ASC 
		
	sql_attr_uint 		= ind
	sql_attr_uint 		= pay
	sql_attr_uint 		= type
	sql_attr_string		= showStatus
}

source lvivmarket_tender : lvivmarket_base
{

	sql_query_pre 		= SET NAMES utf8
	sql_query_range 	= SELECT MIN(id), MAX(id) FROM `tender` 
	sql_range_step 		= 500
	
	sql_query = \
		select \
			id, \
			title, \
			if(type='buy', 1, 2) as type, \
			nrId, \
			des, \
			idprojekt, \
			if(status='wait', 1, 2) as status, \
			UNIX_TIMESTAMP(`date`) as `date`\
		from tender \
		where id >= $start AND id <= $end \
		ORDER BY id ASC 
	
	sql_attr_uint 		= type	
	sql_attr_uint 		= nrId
	sql_attr_uint 		= status
	sql_attr_uint 		= idprojekt
	sql_attr_timestamp 	= date 
	
}

###---------- LVIVMARKET
###------------------------------------------------------------------------

#############################################################################
## index definition
#############################################################################

###------------------------------------------------------------------------
###---------- OBUVUA

index obuvua_goods
{
	source 			= obuvua_goods
    path 			= /home/vagrant/projects/sphinx/data/obuvua_goods
	# Ukrainian chars
    charset_table = 0..9, U+002E, _, A..Z->a..z, a..z, U+410..U+42F->U+430..U+44F, U+430..U+44F, U+404->U+454, U+454, U+406->U+456, U+456, U+407->U+457, U+457, U+490->U+491, U+491, U+401->U+435, U+451->U+435, U+002D
    morphology 		= stem_enru
    min_word_len 	= 2
	html_strip 		= 1
	#expand_keywords = 1
}

index obuvua_nr
{
	source 			= obuvua_nr
    path 			= /home/vagrant/projects/sphinx/data/obuvua_nr
    morphology 		= stem_enru
    min_word_len 	= 3
	html_strip 		= 0
}

index obuvua_goodscat
{
	source 			= obuvua_goodscat
    path 			= /home/vagrant/projects/sphinx/data/obuvua_goodscat
	# Ukrainian chars
    charset_table = 0..9, _, A..Z->a..z, a..z, U+410..U+42F->U+430..U+44F, U+430..U+44F, U+404->U+454, U+454, U+406->U+456, U+456, U+407->U+457, U+457, U+490->U+491, U+491, U+401->U+435, U+451->U+435, U+002D
    morphology 		= stem_enru
    min_word_len 	= 3
	html_strip 		= 0
}

index obuvua_projekt
{
	source 			= obuvua_projekt
    path 			= /home/vagrant/projects/sphinx/data/obuvua_projekt
	# Ukrainian chars
    charset_table = 0..9, _, A..Z->a..z, a..z, U+410..U+42F->U+430..U+44F, U+430..U+44F, U+404->U+454, U+454, U+406->U+456, U+456, U+407->U+457, U+457, U+490->U+491, U+491, U+401->U+435, U+451->U+435, U+002D
    morphology 		= stem_enru
    min_word_len 	= 3
	html_strip 		= 0
}

###---------- OBUVUA
###------------------------------------------------------------------------


###------------------------------------------------------------------------
###---------- BIGOPT

index goods
{
	source 			= goods
    path 			= /home/vagrant/projects/sphinx/data/goods
	# Ukrainian chars
    charset_table = 0..9, U+002E, _, A..Z->a..z, a..z, U+410..U+42F->U+430..U+44F, U+430..U+44F, U+404->U+454, U+454, U+406->U+456, U+456, U+407->U+457, U+457, U+490->U+491, U+491, U+401->U+435, U+451->U+435, U+002D
    morphology 		= stem_enru
    min_word_len 	= 2
	html_strip 		= 1
}

index nr
{
	source 			= nr
    path 			= /home/vagrant/projects/sphinx/data/nr
    morphology 		= stem_enru
    min_word_len 	= 3
	html_strip 		= 0
}

index goodscat
{
	source 			= goodscat
    path 			= /home/vagrant/projects/sphinx/data/goodscat
	# Ukrainian chars
    charset_table = 0..9, _, A..Z->a..z, a..z, U+410..U+42F->U+430..U+44F, U+430..U+44F, U+404->U+454, U+454, U+406->U+456, U+456, U+407->U+457, U+457, U+490->U+491, U+491, U+401->U+435, U+451->U+435, U+002D
    morphology 		= stem_enru
    min_word_len 	= 3
	html_strip 		= 0
}

index informer
{
	source 			= informer
    path 			= /home/vagrant/projects/sphinx/data/informer
	# Ukrainian chars
    charset_table = 0..9, _, A..Z->a..z, a..z, U+410..U+42F->U+430..U+44F, U+430..U+44F, U+404->U+454, U+454, U+406->U+456, U+456, U+407->U+457, U+457, U+490->U+491, U+491, U+401->U+435, U+451->U+435, U+002D
    morphology 		= stem_enru
    min_word_len 	= 3
	html_strip 		= 0
}

index projekt
{
	source 			= projekt
    path 			= /home/vagrant/projects/sphinx/data/projekt
	# Ukrainian chars
    charset_table = 0..9, _, A..Z->a..z, a..z, U+410..U+42F->U+430..U+44F, U+430..U+44F, U+404->U+454, U+454, U+406->U+456, U+456, U+407->U+457, U+457, U+490->U+491, U+491, U+401->U+435, U+451->U+435, U+002D
    morphology 		= stem_enru
    min_word_len 	= 3
	html_strip 		= 0
}

index wall
{
	source 			= wall
    path 			= /home/vagrant/projects/sphinx/data/wall
	# Ukrainian chars
    charset_table = 0..9, _, A..Z->a..z, a..z, U+410..U+42F->U+430..U+44F, U+430..U+44F, U+404->U+454, U+454, U+406->U+456, U+456, U+407->U+457, U+457, U+490->U+491, U+491, U+401->U+435, U+451->U+435, U+002D
    morphology 		= stem_enru
    min_word_len 	= 3
	html_strip 		= 0
}

###---------- BIGOPT
###------------------------------------------------------------------------


###------------------------------------------------------------------------
###---------- AGROBIZ

index ag_goods
{
	source 			= ag_goods
    path 			= /home/vagrant/projects/sphinx/data/ag_goods
	# Ukrainian chars
    charset_table = 0..9, U+002E, _, A..Z->a..z, a..z, U+410..U+42F->U+430..U+44F, U+430..U+44F, U+404->U+454, U+454, U+406->U+456, U+456, U+407->U+457, U+457, U+490->U+491, U+491, U+401->U+435, U+451->U+435, U+002D
    morphology 		= stem_enru
    min_word_len 	= 3
	html_strip 		= 1
	expand_keywords = 1
}

index ag_nr
{
	source 			= ag_nr
    path 			= /home/vagrant/projects/sphinx/data/ag_nr
    morphology 		= stem_enru
    min_word_len 	= 3
	html_strip 		= 0
}

index ag_goodscat
{
	source 			= ag_goodscat
    path 			= /home/vagrant/projects/sphinx/data/ag_goodscat
	# Ukrainian chars
    charset_table = 0..9, _, A..Z->a..z, a..z, U+410..U+42F->U+430..U+44F, U+430..U+44F, U+404->U+454, U+454, U+406->U+456, U+456, U+407->U+457, U+457, U+490->U+491, U+491, U+401->U+435, U+451->U+435, U+002D
    morphology 		= stem_enru
    min_word_len 	= 3
	html_strip 		= 0
}

index ag_informer
{
	source 			= ag_informer
    path 			= /home/vagrant/projects/sphinx/data/ag_informer
	# Ukrainian chars
    charset_table = 0..9, _, A..Z->a..z, a..z, U+410..U+42F->U+430..U+44F, U+430..U+44F, U+404->U+454, U+454, U+406->U+456, U+456, U+407->U+457, U+457, U+490->U+491, U+491, U+401->U+435, U+451->U+435, U+002D
    morphology 		= stem_enru
    min_word_len 	= 3
	html_strip 		= 0
}

index ag_projekt
{
	source 			= ag_projekt
    path 			= /home/vagrant/projects/sphinx/data/ag_projekt
	# Ukrainian chars
    charset_table = 0..9, _, A..Z->a..z, a..z, U+410..U+42F->U+430..U+44F, U+430..U+44F, U+404->U+454, U+454, U+406->U+456, U+456, U+407->U+457, U+457, U+490->U+491, U+491, U+401->U+435, U+451->U+435, U+002D
    morphology 		= stem_enru
    min_word_len 	= 3
	html_strip 		= 0
}
 
index ag_text
{
	source 			= ag_text
    path 			= /home/vagrant/projects/sphinx/data/ag_text
	# Ukrainian chars
    charset_table = 0..9, _, A..Z->a..z, a..z, U+410..U+42F->U+430..U+44F, U+430..U+44F, U+404->U+454, U+454, U+406->U+456, U+456, U+407->U+457, U+457, U+490->U+491, U+491, U+401->U+435, U+451->U+435, U+002D
    morphology 		= stem_enru
    min_word_len 	= 3
	html_strip 		= 0
}

index ag_advert
{
	source 			= ag_advert
    path 			= /home/vagrant/projects/sphinx/data/ag_advert
	# Ukrainian chars
    charset_table = 0..9, _, A..Z->a..z, a..z, U+410..U+42F->U+430..U+44F, U+430..U+44F, U+404->U+454, U+454, U+406->U+456, U+456, U+407->U+457, U+457, U+490->U+491, U+491, U+401->U+435, U+451->U+435, U+002D
    morphology 		= stem_enru
    min_word_len 	= 3
	html_strip 		= 0
}




###---------- AGROBIZ
###------------------------------------------------------------------------

###------------------------------------------------------------------------
###---------- PODKAPOT

index kapot_filter
{
	source 			= kapot_filter
    path 			= /home/vagrant/projects/sphinx/data/kapot_filter
    morphology 		= none
    min_word_len 	= 3
	html_strip 		= 1
	expand_keywords = 1
}

index kapot_goods
{
	source 			= kapot_goods
    path 			= /home/vagrant/projects/sphinx/data/kapot_goods
	# Ukrainian chars 
    charset_table = 0..9, U+002E, _, A..Z->a..z, a..z, U+410..U+42F->U+430..U+44F, U+430..U+44F, U+404->U+454, U+454, U+406->U+456, U+456, U+407->U+457, U+457, U+490->U+491, U+491, U+401->U+435, U+451->U+435, U+002D
    morphology 		= stem_enru
    min_word_len 	= 2
	html_strip 		= 1
	expand_keywords = 1
}

index kapot_nr
{
	source 			= kapot_nr
    path 			= /home/vagrant/projects/sphinx/data/kapot_nr
    morphology 		= stem_enru
    min_word_len 	= 3
	html_strip 		= 0
}

index kapot_goodscat
{
	source 			= kapot_goodscat
    path 			= /home/vagrant/projects/sphinx/data/kapot_goodscat
	# Ukrainian chars
    charset_table = 0..9, _, A..Z->a..z, a..z, U+410..U+42F->U+430..U+44F, U+430..U+44F, U+404->U+454, U+454, U+406->U+456, U+456, U+407->U+457, U+457, U+490->U+491, U+491, U+401->U+435, U+451->U+435, U+002D
    morphology 		= stem_enru
    min_word_len 	= 3
	html_strip 		= 0
}

index kapot_price
{
	source 			= kapot_price
    path 			= /home/vagrant/projects/sphinx/data/kapot_price
	# Ukrainian chars
    charset_table = 0..9, _, A..Z->a..z, a..z, U+410..U+42F->U+430..U+44F, U+430..U+44F, U+404->U+454, U+454, U+406->U+456, U+456, U+407->U+457, U+457, U+490->U+491, U+491, U+401->U+435, U+451->U+435, U+002D
    morphology 		= stem_enru
    min_word_len 	= 3
	html_strip 		= 0
}

#index kapot_informer
#{
#	source 			= kapot_informer
#    path 			= /home/vagrant/projects/sphinx/data/kapot_informer
#    morphology 		= stem_enru
#    min_word_len 	= 3
#	html_strip 		= 0
#}

index kapot_projekt
{
	source 			= kapot_projekt
    path 			= /home/vagrant/projects/sphinx/data/kapot_projekt
	# Ukrainian chars
    charset_table = 0..9, _, A..Z->a..z, a..z, U+410..U+42F->U+430..U+44F, U+430..U+44F, U+404->U+454, U+454, U+406->U+456, U+456, U+407->U+457, U+457, U+490->U+491, U+491, U+401->U+435, U+451->U+435, U+002D
    morphology 		= stem_enru
    min_word_len 	= 3
	html_strip 		= 0
	expand_keywords = 1
}

###---------- PODKAPOT
###------------------------------------------------------------------------


###------------------------------------------------------------------------
###---------- NAVOLYNI

index navolyni_goods
{
	source 			= navolyni_goods
    path 			= /home/vagrant/projects/sphinx/data/navolyni_goods
	# Ukrainian chars
    charset_table = 0..9, U+002E, _, A..Z->a..z, a..z, U+410..U+42F->U+430..U+44F, U+430..U+44F, U+404->U+454, U+454, U+406->U+456, U+456, U+407->U+457, U+457, U+490->U+491, U+491, U+401->U+435, U+451->U+435, U+002D
    morphology 		= stem_enru
    min_word_len 	= 3
	html_strip 		= 1
}

index navolyni_nr
{
	source 			= navolyni_nr
    path 			= /home/vagrant/projects/sphinx/data/navolyni_nr
    morphology 		= stem_enru
    min_word_len 	= 3
	html_strip 		= 0
}

index navolyni_goodscat
{
	source 			= navolyni_goodscat
    path 			= /home/vagrant/projects/sphinx/data/navolyni_goodscat
	# Ukrainian chars
    charset_table = 0..9, _, A..Z->a..z, a..z, U+410..U+42F->U+430..U+44F, U+430..U+44F, U+404->U+454, U+454, U+406->U+456, U+456, U+407->U+457, U+457, U+490->U+491, U+491, U+401->U+435, U+451->U+435, U+002D
    morphology 		= stem_enru
    min_word_len 	= 3
	html_strip 		= 0
}

index navolyni_informer
{
	source 			= navolyni_informer
    path 			= /home/vagrant/projects/sphinx/data/navolyni_informer
	# Ukrainian chars
    charset_table = 0..9, _, A..Z->a..z, a..z, U+410..U+42F->U+430..U+44F, U+430..U+44F, U+404->U+454, U+454, U+406->U+456, U+456, U+407->U+457, U+457, U+490->U+491, U+491, U+401->U+435, U+451->U+435, U+002D
    morphology 		= stem_enru
    min_word_len 	= 3
	html_strip 		= 0
}

index navolyni_projekt
{
	source 			= navolyni_projekt
    path 			= /home/vagrant/projects/sphinx/data/navolyni_projekt
	# Ukrainian chars
    charset_table = 0..9, _, A..Z->a..z, a..z, U+410..U+42F->U+430..U+44F, U+430..U+44F, U+404->U+454, U+454, U+406->U+456, U+456, U+407->U+457, U+457, U+490->U+491, U+491, U+401->U+435, U+451->U+435, U+002D
    morphology 		= stem_enru
    min_word_len 	= 3
	html_strip 		= 0
}

index navolyni_tender
{
	source 			= navolyni_tender
    path 			= /home/vagrant/projects/sphinx/data/navolyni_tender
	# Ukrainian chars
    charset_table = 0..9, _, A..Z->a..z, a..z, U+410..U+42F->U+430..U+44F, U+430..U+44F, U+404->U+454, U+454, U+406->U+456, U+456, U+407->U+457, U+457, U+490->U+491, U+491, U+401->U+435, U+451->U+435, U+002D
    morphology 		= stem_enru
    min_word_len 	= 3
	html_strip 		= 0
}



###---------- NAVOLYNI
###------------------------------------------------------------------------

###------------------------------------------------------------------------
###---------- LVIVMARKET

index lvivmarket_goods
{
	source 			= lvivmarket_goods
    path 			= /home/vagrant/projects/sphinx/data/lvivmarket_goods
	# Ukrainian chars
    charset_table = 0..9, U+002E, _, A..Z->a..z, a..z, U+410..U+42F->U+430..U+44F, U+430..U+44F, U+404->U+454, U+454, U+406->U+456, U+456, U+407->U+457, U+457, U+490->U+491, U+491, U+401->U+435, U+451->U+435, U+002D
    morphology 		= stem_enru
    min_word_len 	= 3
	html_strip 		= 1
}

index lvivmarket_nr
{
	source 			= lvivmarket_nr
    path 			= /home/vagrant/projects/sphinx/data/lvivmarket_nr
    morphology 		= stem_enru
    min_word_len 	= 3
	html_strip 		= 0
}

index lvivmarket_goodscat
{
	source 			= lvivmarket_goodscat
    path 			= /home/vagrant/projects/sphinx/data/lvivmarket_goodscat
	# Ukrainian chars
    charset_table = 0..9, _, A..Z->a..z, a..z, U+410..U+42F->U+430..U+44F, U+430..U+44F, U+404->U+454, U+454, U+406->U+456, U+456, U+407->U+457, U+457, U+490->U+491, U+491, U+401->U+435, U+451->U+435, U+002D
    morphology 		= stem_enru
    min_word_len 	= 3
	html_strip 		= 0
}

index lvivmarket_informer
{
	source 			= lvivmarket_informer
    path 			= /home/vagrant/projects/sphinx/data/lvivmarket_informer
	# Ukrainian chars
    charset_table = 0..9, _, A..Z->a..z, a..z, U+410..U+42F->U+430..U+44F, U+430..U+44F, U+404->U+454, U+454, U+406->U+456, U+456, U+407->U+457, U+457, U+490->U+491, U+491, U+401->U+435, U+451->U+435, U+002D
    morphology 		= stem_enru
    min_word_len 	= 3
	html_strip 		= 0
}

index lvivmarket_projekt
{
	source 			= lvivmarket_projekt
    path 			= /home/vagrant/projects/sphinx/data/lvivmarket_projekt
	# Ukrainian chars
    charset_table = 0..9, _, A..Z->a..z, a..z, U+410..U+42F->U+430..U+44F, U+430..U+44F, U+404->U+454, U+454, U+406->U+456, U+456, U+407->U+457, U+457, U+490->U+491, U+491, U+401->U+435, U+451->U+435, U+002D
    morphology 		= stem_enru
    min_word_len 	= 3
	html_strip 		= 0
}

index lvivmarket_tender
{
	source 			= lvivmarket_tender
    path 			= /home/vagrant/projects/sphinx/data/lvivmarket_tender
	# Ukrainian chars
    charset_table = 0..9, _, A..Z->a..z, a..z, U+410..U+42F->U+430..U+44F, U+430..U+44F, U+404->U+454, U+454, U+406->U+456, U+456, U+407->U+457, U+457, U+490->U+491, U+491, U+401->U+435, U+451->U+435, U+002D
    morphology 		= stem_enru
    min_word_len 	= 3
	html_strip 		= 0
}



###---------- LVIVMARKET
###------------------------------------------------------------------------


#############################################################################
## indexer settings
#############################################################################

indexer
{
	# memory limit, in bytes, kiloytes (16384K) or megabytes (256M)
	# optional, default is 128M, max is 2047M, recommended is 256M to 1024M
	mem_limit		= 512M

	# maximum IO calls per second (for I/O throttling)
	# optional, default is 0 (unlimited)
	#
	# max_iops		= 40


	# maximum IO call size, bytes (for I/O throttling)
	# optional, default is 0 (unlimited)
	#
	# max_iosize		= 1048576


	# maximum xmlpipe2 field length, bytes
	# optional, default is 2M
	#
	# max_xmlpipe2_field	= 4M


	# write buffer size, bytes
	# several (currently up to 4) buffers will be allocated
	# write buffers are allocated in addition to mem_limit
	# optional, default is 1M
	#
	# write_buffer		= 1M


	# maximum file field adaptive buffer size
	# optional, default is 8M, minimum is 1M
	#
	# max_file_field_buffer	= 32M


	# how to handle IO errors in file fields
	# known values are 'ignore_field', 'skip_document', and 'fail_index'
	# optional, default is 'ignore_field'
	#
	# on_file_field_error = skip_document


	# how to handle syntax errors in JSON attributes
	# known values are 'ignore_attr' and 'fail_index'
	# optional, default is 'ignore_attr'
	#
	# on_json_attr_error = fail_index


	# whether to auto-convert numeric values from strings in JSON attributes
	# with auto-conversion, string value with actually numeric data
	# (as in {"key":"12345"}) gets stored as a number, rather than string
	# optional, allowed values are 0 and 1, default is 0 (do not convert)
	#
	# json_autoconv_numbers = 1


	# whether and how to auto-convert key names in JSON attributes
	# known value is 'lowercase'
	# optional, default is unspecified (do nothing)
	#
	# json_autoconv_keynames = lowercase


	# lemmatizer cache size
	# improves the indexing time when the lemmatization is enabled
	# optional, default is 256K
	#
	# lemmatizer_cache = 512M
}

#############################################################################
## searchd settings
#############################################################################

searchd
{
	# [hostname:]port[:protocol], or /unix/socket/path to listen on
	# known protocols are 'sphinx' (SphinxAPI) and 'mysql41' (SphinxQL)
	#
	# multi-value, multiple listen points are allowed
	# optional, defaults are 9312:sphinx and 9306:mysql41, as below
	#
	# listen			= 127.0.0.1
	# listen			= 192.168.0.1:9312
	# listen			= 9312
	# listen			= /var/run/searchd.sock
	listen			= 9312
	listen			= 9306:mysql41

	# log file, searchd run info is logged here
	# optional, default is 'searchd.log'
	log			= /home/vagrant/projects/sphinx/data/searchd.log

	# query log file, all search queries are logged here
	# optional, default is empty (do not log queries)
	query_log		= /home/vagrant/projects/sphinx/data/sphinx-query.log

	# client read timeout, seconds
	# optional, default is 5
	read_timeout		= 5

	# request timeout, seconds
	# optional, default is 5 minutes
	client_timeout		= 300

	# maximum amount of children to fork (concurrent searches to run)
	# optional, default is 0 (unlimited)
	max_children		= 30

	# maximum amount of persistent connections from this master to each agent host
	# optional, but necessary if you use agent_persistent. It is reasonable to set the value
	# as max_children, or less on the agent's hosts.
	persistent_connections_limit	= 30

	# PID file, searchd process ID file name
	# mandatory
	pid_file		= /home/vagrant/projects/sphinx/searchd.pid

	# seamless rotate, prevents rotate stalls if precaching huge datasets
	# optional, default is 1
	seamless_rotate		= 1

	# whether to forcibly preopen all indexes on startup
	# optional, default is 1 (preopen everything)
	preopen_indexes		= 1

	# whether to unlink .old index copies on succesful rotation.
	# optional, default is 1 (do unlink)
	unlink_old		= 1

	# attribute updates periodic flush timeout, seconds
	# updates will be automatically dumped to disk this frequently
	# optional, default is 0 (disable periodic flush)
	#
	# attr_flush_period	= 900


	# MVA updates pool size
	# shared between all instances of searchd, disables attr flushes!
	# optional, default size is 1M
	mva_updates_pool	= 1M

	# max allowed network packet size
	# limits both query packets from clients, and responses from agents
	# optional, default size is 8M
	max_packet_size		= 8M

	# max allowed per-query filter count
	# optional, default is 256
	max_filters		= 256

	# max allowed per-filter values count
	# optional, default is 4096
	max_filter_values	= 4096


	# socket listen queue length
	# optional, default is 5
	#
	# listen_backlog		= 5


	# per-keyword read buffer size
	# optional, default is 256K
	#
	# read_buffer		= 256K


	# unhinted read size (currently used when reading hits)
	# optional, default is 32K
	#
	# read_unhinted		= 32K


	# max allowed per-batch query count (aka multi-query count)
	# optional, default is 32
	max_batch_queries	= 32


	# max common subtree document cache size, per-query
	# optional, default is 0 (disable subtree optimization)
	#
	# subtree_docs_cache	= 4M


	# max common subtree hit cache size, per-query
	# optional, default is 0 (disable subtree optimization)
	#
	# subtree_hits_cache	= 8M


	# multi-processing mode (MPM)
	# known values are none, fork, prefork, and threads
	# threads is required for RT backend to work
	# optional, default is threads
	workers			= threads # for RT to work


	# max threads to create for searching local parts of a distributed index
	# optional, default is 0, which means disable multi-threaded searching
	# should work with all MPMs (ie. does NOT require workers=threads)
	#
	# dist_threads		= 4


	# binlog files path; use empty string to disable binlog
	# optional, default is build-time configured data directory
	#
	# binlog_path		= # disable logging
	# binlog_path		= /var/db/sphinxsearch/data # binlog.001 etc will be created there
	binlog_path		= 

	# binlog flush/sync mode
	# 0 means flush and sync every second
	# 1 means flush and sync every transaction
	# 2 means flush every transaction, sync every second
	# optional, default is 2
	#
	# binlog_flush		= 2


	# binlog per-file size limit
	# optional, default is 128M, 0 means no limit
	#
	# binlog_max_log_size	= 256M


	# per-thread stack size, only affects workers=threads mode
	# optional, default is 64K
	#
	# thread_stack			= 128K


	# per-keyword expansion limit (for dict=keywords prefix searches)
	# optional, default is 0 (no limit)
	#
	# expansion_limit		= 1000


	# RT RAM chunks flush period
	# optional, default is 0 (no periodic flush)
	#
	# rt_flush_period		= 900


	# query log file format
	# optional, known values are plain and sphinxql, default is plain
	#
	# query_log_format		= sphinxql


	# version string returned to MySQL network protocol clients
	# optional, default is empty (use Sphinx version)
	#
	# mysql_version_string	= 5.0.37


	# trusted plugin directory
	# optional, default is empty (disable UDFs)
	#
	# plugin_dir			= /usr/local/sphinx/lib


	# default server-wide collation
	# optional, default is libc_ci
	#
	# collation_server		= utf8_general_ci


	# server-wide locale for libc based collations
	# optional, default is C
	#
	# collation_libc_locale	= ru_RU.UTF-8


	# threaded server watchdog (only used in workers=threads mode)
	# optional, values are 0 and 1, default is 1 (watchdog on)
	#
	# watchdog				= 1

	
	# costs for max_predicted_time model, in (imaginary) nanoseconds
	# optional, default is "doc=64, hit=48, skip=2048, match=64"
	#
	# predicted_time_costs	= doc=64, hit=48, skip=2048, match=64


	# current SphinxQL state (uservars etc) serialization path
	# optional, default is none (do not serialize SphinxQL state)
	#
	# sphinxql_state			= sphinxvars.sql


	# maximum RT merge thread IO calls per second, and per-call IO size
	# useful for throttling (the background) OPTIMIZE INDEX impact
	# optional, default is 0 (unlimited)
	#
	# rt_merge_iops			= 40
	# rt_merge_maxiosize		= 1M


	# interval between agent mirror pings, in milliseconds
	# 0 means disable pings
	# optional, default is 1000
	#
	# ha_ping_interval		= 0


	# agent mirror statistics window size, in seconds
	# stats older than the window size (karma) are retired
	# that is, they will not affect master choice of agents in any way
	# optional, default is 60 seconds
	#
	# ha_period_karma			= 60


	# delay between preforked children restarts on rotation, in milliseconds
	# optional, default is 0 (no delay)
	#
	# prefork_rotation_throttle	= 100


	# a prefix to prepend to the local file names when creating snippets
	# with load_files and/or load_files_scatter options
	# optional, default is empty
	#
	# snippets_file_prefix		= /mnt/common/server1/
}

#############################################################################
## common settings
#############################################################################

common
{

	# lemmatizer dictionaries base path
	# optional, defaut is /usr/local/share (see ./configure --datadir)
	#
	# lemmatizer_base = /usr/local/share/sphinx/dicts

	# path to RLP root directory
	# optional, defaut is /usr/local/share (see ./configure --datadir)
	#
	# rlp_root = /usr/local/share/sphinx/rlp

	# path to RLP environment file
	# optional, defaut is /usr/local/share/rlp-environment.xml (see ./configure --datadir)
	#
	# rlp_environment = /usr/local/share/sphinx/rlp/rlp/etc/rlp-environment.xml
}

# --eof--
